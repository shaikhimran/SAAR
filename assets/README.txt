Save your SAAR Aroma poster image in this folder as:

    combo.jpg

It will appear automatically as the hero image on the landing page.
If the file is missing, the page shows a placeholder and still works.

Tip: keep the image under ~400 KB for fast loading (you can also use combo.png and rename the <img src> in index.html).
